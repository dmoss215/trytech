<div class="container register-container">

	{{#if errors}}
	      {{#each errors}}
	        <div class="alert alert-danger">{{msg}}</div>
	      {{/each}}
	{{/if}}
	<br>
	<h2 class="welcome-header text-center">Welcome to the TryTech family, {{ firstName }} {{ lastName }}!</h2>
	<br>
	<h4 class="text-center">The email associated with your account is "{{ email }}".</h4>
	<br>
	<h4 class="text-center">Just a few quick steps to finalize your account.</h4>
	<h4 class="text-center">We'll need to know where to ship your awesome new tech.</h4>
	<h4 class="text-center">You can skip this step if you like and start using TryTech right now.</h4>
	<br>
	<form method="post" action="/welcome/user">
		<input type="hidden" name="firstName" value="{{ firstName }}"/>
		<input type="hidden" name="lastName" value="{{ lastName }}"/>
		<input type="hidden" name="email" value="{{ email }}"/>
		<input type="hidden" name="password" value="{{ password }}"/>
		<div>
	  	<label class="custom-control custom-checkbox">
		  	<input type="checkbox" class="form-check-input" name="agree" required>
		  	I agree to the terms and conditions of the TryTech Customer Agreement. (required field)
			</label>
	  </div>
		<a href="/"><button class="btn btn-default register-submit-button">Take me to Trytech now!</button><a>
	  <br>
	  <br>
	  <div class="form-group">
	    <label>Street Address</label>
	    <input type="text" class="form-control" placeholder="ex: 123 Main St." name="addressStreet">
	  </div>
	  <div class="form-group">
	    <label>City</label>
	    <input type="text" class="form-control" placeholder="City" name="addressCity">
	  </div>
	   <div class="form-group">
	    <label>State</label>
	    <input type="text" class="form-control" placeholder="State" name="addressState">
	  </div>
	  <div class="form-group">
	    <label>Zip</label>
	    <input type="text" class="form-control" placeholder="Zip Code" name="addressZip">
	  </div>
	  <button type="submit" class="btn btn-default register-submit-btn">Submit</button>
	</form>

</div>